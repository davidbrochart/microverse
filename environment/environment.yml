name: my-env 
channels:
  - https://repo.prefix.dev/emscripten-forge-dev
  - https://repo.prefix.dev/conda-forge
dependencies:
  # server dependencies:
  - pyjs
  - httpx >=0.28.1
  - httpx-ws >=0.7.2
  - jupyverse-api >=0.11.0
  - fps-frontend >=0.9.0
  - fps-lab >=0.9.0
  - fps-jupyterlab >=0.9.0
  - fps-noauth >=0.9.0
  - fps-contents >=0.10.0
  - fps-kernels >=0.9.1
  - fps-akernel-task >=0.1.3
  - fps-kernel-web-worker >=0.1.3
  - akernel >=0.3.4

  # kernel dependencies:
  - numpy
  - pandas
  - pydro
